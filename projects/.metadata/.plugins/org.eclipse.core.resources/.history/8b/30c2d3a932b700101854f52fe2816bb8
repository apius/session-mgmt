package org.apius.messaging.atom;

import org.apius.spring.ApplicationContextProvider;
import org.restlet.data.MediaType;
import org.restlet.ext.atom.Entry;
import org.restlet.ext.atom.Text;
import org.springframework.context.ApplicationContext;

public class AtomProtocolBeansFactory {

    private ApplicationContext ctx = ApplicationContextProvider.getApplicationContext();
    
    public Entry createEntry() {
        Entry entry = (Entry) ctx.getBean("atomEntry");
        
        return entry;
    }
    
    public Text createText(String content) {
        Text text = (Text) ctx.getBean("atomText");
        text.setContent(content);
        
        return text;
    }
    
    public Text createText(MediaType type) {
        Text text = (Text) ctx.getBean("atomText");
        text.setType(type);
        
        return text;
    }
    
    public Text createText(MediaType type, String content) {
        Text text = (Text) applicationContext.getBean("atomText");
        text.setContent(content);
        text.setType(type);
        
        return text;
    }
    
}
